import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const DisciplineOverview = () => (
  <Card className="m-6">
    <CardHeader>
      <CardTitle>Discipline Overview</CardTitle>
      <CardDescription>Team leader view — manage projects and team hours by discipline. Coming in Phase 3.</CardDescription>
    </CardHeader>
  </Card>
);

export default DisciplineOverview;
