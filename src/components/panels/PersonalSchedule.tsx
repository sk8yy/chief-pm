import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const PersonalSchedule = () => (
  <Card className="m-6">
    <CardHeader>
      <CardTitle>Personal Work Schedule</CardTitle>
      <CardDescription>Your weekly calendar with discipline-colored project rows, hour entry, tasks, and summaries. Coming in Phase 2.</CardDescription>
    </CardHeader>
  </Card>
);

export default PersonalSchedule;
