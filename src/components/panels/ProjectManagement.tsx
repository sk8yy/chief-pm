import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

const ProjectManagement = () => (
  <Card className="m-6">
    <CardHeader>
      <CardTitle>Project Management</CardTitle>
      <CardDescription>PM view — manage team hours, tasks, and compare planned vs. recorded per project. Coming in Phase 4.</CardDescription>
    </CardHeader>
  </Card>
);

export default ProjectManagement;
